/** Every station a workpiece can be at, plus NONE for in transit. */
public enum Machine {
	LOADER, CONVEYOR, ROTARY_TABLE, FILLER, LID_LOADER, LID_CAPPER, LABELLER,
	BATCH_STORAGE, SPLITTER, RECYCLING, NONE
}
